# Workspace Instructions

- Use Next.js App Router with TypeScript.
- Keep the MTD calculation and Excel export on the server so the UI and the download use the same source of truth.
- Prefer Prisma for all database access.
- Keep styling intentional and spreadsheet-oriented, not generic dashboard UI.
- Preserve the table layout and export structure close to the provided reference image.
